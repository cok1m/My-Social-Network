let initialState = {
  friends: [
    {name: "Andrew"},
    {name: "Sasha"},
    {name: "Oleg"}
  ]
}

const navbarReducer = (state = initialState, action) => {


  return state;
}

export default navbarReducer